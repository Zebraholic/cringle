!!!
%html
  %head
    %title Cringle - Share Interesting Stuff
  %body
    %nav.navbar.navbar-default.navbar-fixed-top.back
      .container
        / Brand and toggle get grouped for better mobile display
        .navbar-header
          %button.navbar-toggle.collapsed{"data-target" => "#bs-example-navbar-collapse-1", "data-toggle" => "collapse", :type => "button"}
            %span.sr-only Toggle navigation
            %span.icon-bar
            %span.icon-bar
            %span.icon-bar
          %ul.navbar-brand
            = link_to root_path do
              = image_tag "/logo_cringle1.svg"
        / Collect the nav links, forms, and other content for toggling
        #bs-example-navbar-collapse-1.collapse.navbar-collapse
          %form.navbar-form.navbar-right{:role => "search"}
            .form-group
              %input.form-control{:placeholder => "Search", :type => "text"}
            %button.btn.btn-default{:type => "submit"} Submit
          - if user_signed_in?
            %ul.nav.navbar-nav.navbar-right
              %li
                = link_to "Add Item", new_pin_path, class: "white"
              %li
                = link_to "My Profile", edit_user_registration_path, class: "white"
              %li
                = link_to "Sign Out", destroy_user_session_path, method: :delete, class: "white"
              %li
                = image_tag current_user.avatar.url(:thumb), class: "round-image-30"
          - else
            %ul.nav.navbar-nav.navbar-right
              %li
                = link_to "Sign Up", new_user_registration_path, class: "white"
              %li
                = link_to "Sign In", new_user_session_path, class: "white"
        / /.navbar-collapse
      / /.container-fluid
    .container
      - flash.each do |name, msg|
        = content_tag :div, msg, class: "alert alert-info"
      = yield
    = stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true
    = javascript_include_tag 'application', 'data-turbolinks-track' => true
    = csrf_meta_tags